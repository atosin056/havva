// document.addEventListener("DOMContentLoaded", function () {
//     // Check if the current URL ends in .php
//     if (window.location.pathname.endsWith(".php")) {
//         // Remove the .php extension from the URL
//         const newUrl = window.location.pathname.replace(".php", "");
        
//         // Update the address bar without reloading the page
//         history.replaceState(null, "", newUrl);
//     }
// });
